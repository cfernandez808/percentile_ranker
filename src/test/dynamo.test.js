/* Not working */

/*****  const request = require('supertest');
const expect  = require('chai').expect;
const comapnyApi = require('../../amplify/backend/function/companyFunction/src/app.js')

describe('AWS Get Requests', () => {
  it('GET scores responds with array length of 47', function () {
    request('https://9yy81uv9dl.execute-api.us-west-2.amazonaws.com/dev')
    .get('/scores/candidate_id')
    .expect(200)
    .end(function (error, result) {
      if (error) {
        return 'Error'
      }

      expect(result.body.data.length).be(47)
    })
  });

  it('GET companies responds with array length of 5', function () {
    request('https://9yy81uv9dl.execute-api.us-west-2.amazonaws.com/dev')
    .get('/companies/company_id')
    .expect(200)
    .end(function (error, result) {
      if (error) {
        return 'Error'
      }

      expect(result.body.data.length).be(5)
    })
  });
}) *****/
